server:
  port: 8081

spring:
  application:
    name: item-service
  datasource:
    url: jdbc:mysql:///leyou?useUnicode=true&amp&characterEncoding=utf-8
    username: root
    password: 1124219379
    driver-class-name: com.mysql.jdbc.Driver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
  instance:
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 15

mybatis:
  type-aliases-package: com.leyou.item.pojo